import { Injectable } from '@nestjs/common';

const pizz = [2,3,]

@Injectable()
export class AppService {
  root(): string {
    
    // is this the real life?
const pizz = [3,4,];
    return 'Hello World!';
  }
}
